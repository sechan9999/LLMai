# ── Dynatrace ────────────────────────────────────────────────────────────────
# Your Dynatrace OTLP endpoint, e.g. https://abc12345.live.dynatrace.com
DT_ENDPOINT=

# API token with these ingest scopes:
#   openTelemetryTrace.ingest, metrics.ingest, logs.ingest
DT_API_TOKEN=

# Optional — labels every span with deployment.environment
DEPLOYMENT_ENV=dev

# ── llmai agent (these go to the Python process, not docker) ──────────────
# Tell the agent to emit telemetry:
#   LLMAI_OTEL_ENABLED=true
#   LLMAI_OTEL_ENDPOINT=http://localhost:4318
#   LLMAI_OTEL_SERVICE_NAME=llmai-agent
#   LLMAI_OTEL_CONSOLE=true     # also dump spans to stdout (dev only)
