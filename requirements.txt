requests>=2.31
rich>=13.0
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# Optional: telemetry (opt-in via LLMAI_OTEL_ENABLED=true).
# Safe to remove if you don't need observability.
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27

# Optional: persistent memory backed by MongoDB Atlas (opt-in via
# LLMAI_MEMORY_ENABLED=true). Safe to remove if you don't need
# cross-session continuity.
pymongo[srv]>=4.8

# Optional: organizational knowledge search via Elasticsearch
# (opt-in via LLMAI_ELASTIC_ENABLED=true). Safe to remove if you
# don't need hybrid search over issues / logs / docs.
elasticsearch>=8.13,<10
